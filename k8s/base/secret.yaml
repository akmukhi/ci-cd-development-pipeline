apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  labels:
    app: app
type: Opaque
stringData:
  # These should be managed via Sealed Secrets, External Secrets Operator, or ArgoCD secrets
  # Example values - replace with actual secrets management
  database.url: "postgresql://localhost:5432/db"
  api.key: "changeme"
